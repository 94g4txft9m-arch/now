# Skopíruj do .env a uprav (súbor .env sa necommituje).
ENVIRONMENT=development
HOST=127.0.0.1
PORT=8000

# Čiarkou oddelené povolené originy pre CORS (Next.js / statika).
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://127.0.0.1:3330
