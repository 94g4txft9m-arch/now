# Skopíruj do .env a uprav (súbor .env sa necommituje).
ENVIRONMENT=development
HOST=127.0.0.1
PORT=8000

# Voliteľné: ďalšie originy pre CORS (vlastná doména, staging…).
# Na Verceli sa k tomu automaticky pridá https://<VERCEL_URL> (preview/deploy)
# a produkcia z VERCEL_PROJECT_PRODUCTION_URL — netreba duplikovať *.vercel.app.
# CORS_ORIGINS=https://moja-domena.sk
